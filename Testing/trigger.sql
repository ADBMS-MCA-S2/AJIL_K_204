CREATE DATABASE admission;
USE admission;
CREATE TABLE student(admNo INT PRIMARY KEY, studName VARCHAR(30) NOT NULL, course VARCHAR(30) NOT NULL, dept VARCHAR(30) NOT NULL);
drop table mca;
CREATE TABLE mca(admNo INT PRIMARY KEY, studName VARCHAR(30) NOT NULL, course VARCHAR(30) NOT NULL);
CREATE TABLE cs(admNo INT PRIMARY KEY, studName VARCHAR(30) NOT NULL, course VARCHAR(30) NOT NULL);
SELECT * FROM student;
SELECT * FROM mca;
SELECT * FROM cs;
INSERT INTO student VALUES(103, "VARUN", "MCA", "MCA");
INSERT INTO student VALUES(123, "VIRAT", "B.tech", "CS");
INSERT INTO student VALUES(153, "VARUN", "MCA", "MCA");